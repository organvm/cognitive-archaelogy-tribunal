## 2024-05-23 - CLI Visual Hierarchy
**Learning:** Users perceive CLI tools as more professional and trustworthy when output is structured with clear visual hierarchy (headers, steps, success/error indicators) rather than raw text streams.
**Action:** Always implement a central UI utility module using `rich` for CLI tools to enforce consistent styling and feedback patterns across all commands.
